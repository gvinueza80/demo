# Demo Web Applications

This repository contains two dependency-free web apps:

- `clock/` - a 24-hour digital clock with multiple configurable alarms.
- `counted/` - a countdown timer with start, pause, reset, per-second tick sounds, end notification, and alarms.

Open either app by launching its `index.html` file in a browser.
